apiVersion: backstage.io/v1alpha1
kind: System
metadata:
  name: auth
  description: Authentication and authorization system
  tags:
    - auth
    - security
    - coinhub
spec:
  owner: platform
  domain: coinhub
---
apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: auth-system
  description: Auth system components
spec:
  targets:
    - ./api/catalog-info.yaml
    - ./service/catalog-info.yaml
    - ./lib/catalog-info.yaml
