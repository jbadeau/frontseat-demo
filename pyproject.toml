[project]
name = "online-boutique"
version = "0.0.0"
requires-python = ">=3.12"

[tool.uv.workspace]
members = [
    "emailservice",
    "recommendationservice",
    "loadgenerator",
    "shoppingassistantservice",
]
