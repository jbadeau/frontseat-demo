apiVersion: frontseat.io/v1
kind: Workspace
metadata:
  name: coinhub
spec:
  execution:
    address: localhost:8980
    instanceName: remote-execution
  plugins:
    otel:
      options:
        endpoint: "localhost:4317"
        insecure: "true"
