apiVersion: backstage.io/v1alpha1
kind: AsyncAPI
metadata:
  name: account-events
  description: Event-driven API for account lifecycle events
  annotations:
    frontseat.io/type: "asyncapi"
  tags:
    - events
    - kafka
    - accounts
    - coinhub
spec:
  type: kafka
  lifecycle: production
  owner: platform
  system: coinhub
